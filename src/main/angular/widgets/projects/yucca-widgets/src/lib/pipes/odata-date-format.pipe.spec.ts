import { OdataDateFormatPipe } from './odata-date-format.pipe';

describe('OdataDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new OdataDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
