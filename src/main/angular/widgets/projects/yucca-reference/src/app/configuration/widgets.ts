import { DatasetWidgets } from './widgets-dataset';
import { TableWidgets } from './widgets-table';
import { ControlWidgets } from './widgets-control';
import { RelatedWidgets } from './widgets-related';

// number in key for 
export const Widgets = {
    "00_dataset": DatasetWidgets,
    "01_table": TableWidgets,
    "02_control": ControlWidgets,
    "related": RelatedWidgets,
}
